# Crash report
Page: /quiz
## Error
runtime: useAuth must be used within an AuthProvider
at src/context/AuthContext.tsx:118:11
```
  116 |   const context = useContext(AuthContext);
  117 |   if (!context) {
> 118 |     throw new Error("useAuth must be used within an AuthProvider");
      |           ^
  119 |   }
  120 |   return context;
```
## Session replay
(recorder off — no interaction events captured; set EMERGENT_OVERLAY_RRWEB=buffer on the pod to get pre-crash timelines)